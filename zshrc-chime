export EDITOR=vim

# Setup ssh agent
if [ $HOSTNAME = "tubular" ]; then
    source ~/.dotfiles/sshfindagent/ssh-find-agent.sh
    ssh-find-agent -a || eval $(ssh-agent) > /dev/null
fi

alias ssh="ssh -o ForwardAgent=yes"
