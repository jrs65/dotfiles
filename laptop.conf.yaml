
- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.ssh/config: "ssh"
    ~/.zshrc-local: "zshrc-laptop"
    ~/.hammerspoon: "hammerspoon"
    ~/.config/karabiner:
      path: "karabiner"
      force: true

